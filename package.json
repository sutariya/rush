{
  "name": "rush-browser",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "serve . -p 3000",
    "start": "serve . -p 3000",
    "vercel:dev": "vercel dev",
    "build": "echo 'No build step required for static site'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "resend": "^3.2.0"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}